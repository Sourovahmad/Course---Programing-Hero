





  const array = [1, 2, 3, 4, 5];


// this is a function
const plus = (x) => x + 2;

//array.map return first permater as element, second one index and third one as full array

// console.log(array.map(el => plus(el)));


// filter return a array with condition , using x as element;

const filter = array.filter(x =>  x > 5);

// console.log(filter);


// find data will return the first value

const find = array.find(x => x == 2)
console.log(find);






